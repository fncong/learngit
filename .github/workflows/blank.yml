name: learn-github-actions

# Run this workflow every time a new commit pushed to your repository
on: [push]

jobs:
  check-bats-version:
    runs-on: ubantu-laster
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
      - run: npm install -g bats
      - run: bats -v